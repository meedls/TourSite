## TravelHub — туристический портал

Современный веб‑сервис на Django для **поиска, конструирования и управления туристическими маршрутами**: города, перелёты, отели, достопримечательности и участники тура в единой системе.

Проект ориентирован на учебные и демонстрационные цели: показывает работу с Django 6.0, кастомной моделью пользователя, связанной предметной областью и адаптированной версткой.

---

## 🚀 Быстрый старт

- **Python**: 3.12+
- **Django**: 6.0
- **База данных по умолчанию**: PostgreSQL

### 1. Клонирование проекта

```bash
git clone <URL-репозитория>
cd Tour
```

### 2. Установка зависимостей

Проект использует Django, драйвер для PostgreSQL и Pillow:

```bash
pip install django psycopg2-binary pillow
```

При желании вы можете вынести зависимости в `requirements.txt`, но это не обязательно для запуска.

### 3. Настройка базы данных

По умолчанию в `tour_project/settings.py` уже настроен **PostgreSQL**:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'tour_db',
        'USER': 'postgres',
        'PASSWORD': '12345',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

- **Рекомендуется**:
  - создать собственную БД и пользователя в PostgreSQL;
  - изменить `NAME`, `USER`, `PASSWORD` под свои значения;
  - убрать секретные данные из репозитория (переменные окружения, `.env` и т.п.).

#### Создание БД в PostgreSQL (пример)

```sql
CREATE DATABASE tour_db;
```

#### Использование SQLite (опционально)

Если вы хотите использовать SQLite только для локальной разработки, замените блок `DATABASES` в `tour_project/settings.py` на:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

### 4. Применение миграций

```bash
python manage.py migrate
```

### 5. Создание суперпользователя

```bash
python manage.py createsuperuser
```

В проекте также предусмотрен заранее созданный администратор:

- **Логин:** `admin`  
- **Пароль:** `admin`

> Для продакшена обязательно поменяйте эти данные.

### 6. Запуск сервера разработки

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/`.

---

## 📁 Структура проекта

```text
Tour/
├── manage.py
├── tour_project/           # Основные настройки проекта
│   ├── settings.py         # Конфигурация Django, БД, статика, локаль
│   └── urls.py             # Глобальная маршрутизация
├── users/                  # Пользователи и аутентификация
│   ├── models.py           # User (кастомная модель), Profile, Friendship
│   ├── views.py            # Вход, регистрация, профиль
│   └── urls.py
├── places/                 # Города и места
│   ├── models.py           # City, Place
│   └── views.py            # Список городов и достопримечательностей
├── flights/                # Авиарейсы
│   ├── models.py           # Airline, Flight
│   └── views.py            # Список, детали, поиск рейсов
├── hotels/                 # Отели и отзывы
│   ├── models.py           # Hotel, HotelImage, HotelService, Review
│   └── views.py            # Список отелей, детали, бронирование, лайки/дизлайки
├── tours/                  # Туры и конструктор маршрутов
│   ├── models.py           # Tour, TourCity, TourFlight, TourHotel и др.
│   └── views.py            # Главная, список туров, детальная, конструктор
├── templates/              # HTML-шаблоны
│   ├── base.html           # Базовый шаблон
│   ├── tours/
│   ├── hotels/
│   ├── flights/
│   └── users/
├── static/                 # Статические файлы
│   ├── css/                # Стили страниц (главная, туры, отели, рейсы и т.п.)
│   ├── js/                 # JS, в т.ч. 3D-модель Земли
│   └── images/             # Изображения
├── media/                  # Загружаемые файлы пользователей
└── front/                  # Исходники и дизайн статических страниц (макеты)
```

Отдельно есть файл `SETUP_3D.md` с инструкциями по работе с 3D‑моделью Земли, используемой на главной странице.

---

## 🎯 Основной функционал

### Уже реализовано

- **Модели домена**:
  - пользователи (`User`, `Profile`, `Friendship`);
  - города и места (`City`, `Place`);
  - авиакомпании и рейсы (`Airline`, `Flight`);
  - отели, услуги, изображения и отзывы (`Hotel`, `HotelImage`, `HotelService`, `Review`);
  - туры и связанные сущности (`Tour`, `TourCity`, `TourFlight`, `TourHotel`, `TourPlace`, `TourParticipant`, `Favorite`, `Booking`).
- **Django Admin** для всех основных моделей.
- **Маршруты и базовые `views`** для пользователей, туров, отелей, рейсов и мест.
- **Адаптированная верстка**:
  - главная страница;
  - конструктор тура;
  - страница отеля с отзывами и оценками;
  - страницы рейсов и списка туров.
- **Система аутентификации** на основе кастомной модели пользователя.
- **Работа со статикой и медиа** (CSS, JS, изображения, загружаемые файлы).
- **Простая логика** лайков/дизлайков отзывов и фильтрации рейсов по параметрам.

### В разработке / планируется

- расширенная логика **конструктора туров** (полноценное сохранение и редактирование);
- полноценная **система бронирования** туров, отелей и рейсов;
- расширенный **поиск и фильтрация** (по городам, датам, бюджету, сервисам);
- **система друзей** и социальные взаимодействия между пользователями;
- **избранное** (туры, отели, места, рейсы);
- полноценные **формы** создания/редактирования объектов (туры, отзывы и др.);
- использование **AJAX** / fetch для динамической загрузки данных;
- **валидация форм** на сервере и клиенте;
- **система уведомлений** (e-mail/внутренние).

---

## 📄 Основные страницы и URL

- **`/`** — главная страница (приложение `tours`, `index`).
- **`/admin/`** — админ‑панель Django.
- **`/tours/`** — список доступных туров.
- **`/tours/<id>/`** — детальная страница тура.
- **`/tours/create/`** — конструктор тура (доступен только авторизованным).
- **`/hotels/`** — список отелей.
- **`/hotels/<id>/`** — страница конкретного отеля.
- **`/flights/`** — список и поиск рейсов.
- **`/users/login/`** — страница входа.
- **`/users/register/`** — регистрация.
- **`/users/profile/`** — профиль пользователя.

Конкретный набор URL может быть расширен в файлах `urls.py` каждого приложения.

---

## 🔑 Доступ в админ‑панель

- URL: `http://127.0.0.1:8000/admin/`

**Тестовые учетные данные** (для локальной разработки):

- **Логин:** `admin`  
- **Пароль:** `admin`

Для реального развёртывания:

- создайте нового суперпользователя через `createsuperuser`;
- поменяйте пароль и/или отключите тестового администратора.

---

## 🛠 Используемые технологии

- **Backend:** Django 6.0 (Python 3.12+)
- **База данных:** PostgreSQL (основная) / SQLite (для разработки при желании)
- **Frontend:** HTML, CSS, JavaScript
- **Иконки:** Font Awesome 6.4.0
- **Работа с изображениями:** Pillow
- **3D‑модель:** WebGL/Three.js (через статические `js` и ассеты в `static/models` и `static/textures`)

---

## 📝 Идеи для дальнейшего развития

1. Полноценный **личный кабинет** с историей поездок, бронированиями и избранным.
2. Интеграция с внешними API (авиарейсы, погода, карты).
3. Система **рекомендаций туров** на основе предпочтений пользователя.
4. Поддержка **нескольких языков** интерфейса (сейчас базовая локаль `ru-ru`).
5. Оптимизация производительности и кеширование часто запрашиваемых данных.

---

## 📞 Контакты

- **Email:** `journey@gmail.ru`  
- **Телефон:** `8 800 000 35 55`

---

**Все права защищены © 2025**

